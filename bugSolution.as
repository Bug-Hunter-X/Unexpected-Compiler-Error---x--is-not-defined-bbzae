public function someMethod():void {
    var x:int;
    // ... some code ...
    trace("Variable x: "+ x);
    // ... some more code ...
    x = 10; // Assign the variable after it has been declared and initialized
}